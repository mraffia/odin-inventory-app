extends layout

block content
  h1 #{item.name}

  p #[strong Description: ] #{item.desc}
  p #[strong Price: ] #{item.price} Geo
  p #[strong Stock: ] #{item.number_in_stock}
  hr
  p #[strong Category: ]
    a(href=item.category.url) #{item.category.name}
